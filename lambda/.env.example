# Copy to .env for local testing. In AWS, set these as Lambda environment variables instead.

NOTIFICATION_API_BASE_URL=https://dev-api.boons.io/api/v1/notification

# Test values — safe to spam while validating the pipeline. Swap to the real
# customer contact only after the flow is confirmed working end to end.
# Live (after testing): OWNER_EMAIL=tacosjaliscovallejo@gmail.com
# Live (after testing): OWNER_PHONE=+17077314087
OWNER_EMAIL=dev@boons.io
OWNER_PHONE=+15709834561
OWNER_PHONE_COUNTRY=US

# hiring-application only
RESUME_S3_BUCKET=tacos-jalisco-applications

# Both functions — browser Origin must match. curl/Postman without this header is rejected.
ALLOWED_ORIGIN=https://tacosjaliscovallejo.com
